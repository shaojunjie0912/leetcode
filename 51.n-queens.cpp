#include <bits/stdc++.h>

#include "include/list_node.hpp"
#include "include/node.hpp"
#include "include/tree_node.hpp"

using namespace std;

// 排列型回溯

// 一定是每一行每一列恰好一个皇后
// 长为 n 的数组 col: 第 i 行的皇后在第 col[i] 列
// 则 col 数组一定是 0 ~ n - 1 的全排列
// 本题额外多了不能同一斜线的要求

// @leet start
class Solution {
public:
    vector<vector<string>> solveNQueens(int n) {}
};
// @leet end

int main() { return 0; }
